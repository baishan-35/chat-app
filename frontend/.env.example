# API基础URL (开发环境可设置，生产环境建议使用相对路径)
# NEXT_PUBLIC_API_URL=http://localhost:3001

# WebSocket服务器URL (开发环境可设置，生产环境建议使用相对路径)
# NEXT_PUBLIC_WS_URL=ws://localhost:3001

# JWT密钥（生产环境必须使用强随机字符串，至少32字符）
# JWT_SECRET=your_strong_jwt_secret_here_must_be_at_least_32_characters